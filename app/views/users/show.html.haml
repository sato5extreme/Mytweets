.contents.row
  .profile_area
    .profile_name
      = @nickname
    .profile_zone
      = @profile
  %p
    = @nickname
    さんの投稿一覧
  - @tweets.each do |tweet|
    = render partial: "tweets/tweet", locals: { tweet: tweet }
  = paginate(@tweets)
